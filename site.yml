---
# Placeholder playbook for repository syntax validation (Phase 0).
# Full bootstrap orchestration is implemented in Phase 1+.
- name: Bootstrap server
  hosts: all
  gather_facts: false
  tasks:
    - name: Placeholder | bootstrap apply not yet implemented
      ansible.builtin.debug:
        msg: Bootstrap apply playbook is not yet implemented.
