---
- name: Security | load platform-specific variables
  ansible.builtin.include_tasks: load_platform_vars.yml

- name: Security | preflight checks complete
  ansible.builtin.debug:
    msg: Security role preflight has no additional host mutation checks in v0.1.0.
