[defaults]
inventory = inventory
roles_path = roles
collections_path = collections
host_key_checking = True
retry_files_enabled = False
stdout_callback = default
interpreter_python = auto_silent
nocows = True

[inventory]
enable_plugins = ansible.builtin.host_list, ansible.builtin.script, ansible.builtin.auto, ansible.builtin.yaml, ansible.builtin.toml, ansible.builtin.ini

[privilege_escalation]
become = False
