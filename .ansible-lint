---
# Phase 0 baseline. Expand as roles and playbooks are added in later phases.
profile: production
exclude_paths:
  - .venv/
  - .git/
  - collections/
  - graphify-out/
# Public product variables intentionally use the bootstrap_* namespace in role defaults.
skip_list:
  - var-naming[no-role-prefix]
